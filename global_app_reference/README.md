# Global App Reference

This directory is reserved for old shared Janus/global app work that should remain available as reference but should not be treated as active crypto-options implementation.

Do not delete legacy work during cleanup. Move files here only after the repo cleanup inventory classifies them as global reference material.

## Batch 3 Reference Move

Batch 3 moves reviewed non-crypto Janus/global app files here while preserving their original relative paths under `global_app_reference/`.

Compatibility wrappers for crypto entrypoints are intentionally not part of this batch; they remain for Batch 4 review.
