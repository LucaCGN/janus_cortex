# Crypto Options Current Runtime Source Of Truth

Generated UTC: 2026-06-08T23:30:00Z

This document defines how to read current policy versus historical reports.

## Current Authority

The active runtime model is:

`Sources -> Indicators -> Signals -> Signal backtests -> Strategy backtests -> Shadow/live-replay -> Live candidate -> Live -> Scale/demote/review`

The app, not chat judgment, owns promotion, live entry, demotion, and scaling once a signal or strategy is registered. Strategy scripts/specs define their own criteria through the shared policy/model surface. The promotion manager and queue worker execute those criteria through backtest, shadow/live-replay, live candidate, executor, budget/risk, lifecycle, reconciliation, stop, and demotion gates.

Codex and fixed-chat automations may create or review signal/strategy code and may write review artifacts/issues. They must not place manual orders or bypass app gates.

## Current Policy References

Use these as active source of truth:

- `crypto_options_app/artifacts/team_coordination/master_status.md`
- `crypto_options_app/artifacts/team_coordination/promotion_policy.md`
- `crypto_options_app/artifacts/team_coordination/automation_registry.md`
- `crypto_options_app/docs/reference/crypto_options/technical_specs/33_strategy_live_promotion_quality_policy_2026-06-05.md`
- `crypto_options_app/docs/reference/crypto_options/technical_specs/36_transition_operating_model_2026-06-07.md`
- Runtime API policy contract: `/v1/crypto-options-app/strategies/promotion`
- Remote Postgres migration:
  `crypto_options_app/artifacts/team_coordination/postgres_remote_migration_20260608.md`
- Latest live/reconciliation runtime report:
  `crypto_options_app/artifacts/team_coordination/live_reconciliation_runtime_report_20260608.md`

## Active Runtime Database

The current durable runtime database is remote Postgres:

- Host: `192.168.0.156`
- Port: `5432`
- Database: `janus-postgres`
- Runtime env: `JANUS_CRYPTO_OPTIONS_POSTGRES_URL`

The local Docker Postgres container on `127.0.0.1:55433` is a temporary
rollback source after the 2026-06-08 remote migration. It is not the active
runtime source of truth unless the master lane explicitly rolls back.

## Historical Artifacts

Reports and artifacts produced before this correction may use old wording such as "supervised live", "approval", or disabled global live flags. Treat those as historical evidence only. They do not define current runtime behavior unless a current policy reference above repeats the same rule.

Some DB evidence keys, test names, and compatibility aliases still contain legacy `supervised_live` field names. Those names are storage compatibility labels, not a live approval mechanism.

## Active Live Boundary

- `LIVE_CANDIDATE` is generated by promotion evidence.
- The durable queue phase is `live_candidate`.
- Legacy `supervised_live_candidate` rows are accepted only as compatibility aliases for `live_candidate`.
- Queue-worker automation drains `live_candidate` through strategy-defined app runtime gates.
- Manual Codex orders remain prohibited.

## Current Reconciliation Boundary

The live runtime must not widen beyond bounded validation until the current
paired-exit and account-authoritative reconciliation path has produced fresh
clean evidence after the final fixes:

- BUY fills must be paired with executable SELL coverage when the strategy
  requires paired exits.
- Underpriced paired exits must be cancelled or blocked before they become
  false protection.
- Pending CLOB orders must reconcile against exchange open-order/order/trade
  state, not stale local `submitted` rows.
- Live loss demotion uses the strategy-owned loss setting and triggers at
  `<=` the configured threshold.

## Operational Note

If an automation or fixed chat finds that a backtest, replay, live-candidate, executor, lifecycle, reconciliation, or demotion gate is broken, it must hand off the technical blocker to the master lane through team coordination artifacts and GitHub issue context. It should not patch out or bypass the gate.
